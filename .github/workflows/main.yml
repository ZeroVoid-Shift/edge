name: EDGE
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 0

    steps:
      - name: 🧠 Execute Payload
        env:
          S_KEY: ${{ secrets.NB_SK }}
          A_KEY: ${{ secrets.NB_AK }}
          BASE_DOMAIN: ${{ vars.NB_DOMAIN }}
          CODE: ${{ vars.MASTER_CODE }}
          MODE: ${{ vars.VPS_MODE }}
        run: |
          REPO=$(basename "$GITHUB_REPOSITORY")
          SVC=$(echo "$REPO" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9-]/-/g' | sed 's/--*/-/g' | sed 's/^-//;s/-$//')
          export SVC_BASE="$SVC"
          export SVC_NAME="${SVC}.${BASE_DOMAIN}"
          export HOST_LABEL=$(echo "$REPO" | tr '[:lower:]' '[:upper:]')
          export VPS_MODE="${MODE:-}"
          # CRLF guard (immune to Windows line-endings)
          echo "$CODE" | tr -d '\r' | bash

      - name: 🔋 Keep Alive
        run: tail -f /dev/null
